// DemoContext is removed. Re-exports only for incremental component migration.
export type {
  AnswerBlock,
  TextBlock,
  PhotoBlock,
  LocationBlock,
  AudioBlock,
  ListBlock,
} from '../lib/database.types'
